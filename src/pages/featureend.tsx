import { BaseLayout } from "src/components/Layouts/BaseLayout";
import { BroadcastEnd } from "src/components/BroadcastEnd";

const featureend = () => {
  return (
    <BaseLayout title="放送済み">
      <BroadcastEnd />
    </BaseLayout>
  );
};

export default featureend;
